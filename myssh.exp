#!/usr/bin/expect -f

set user [exec whoami]
set pwd [lindex $argv 0]
set pwd [regsub {\(.*} $pwd ""]
spawn sudo login $user
set timeout 3
expect "$user login: "
send "$user\r"
set timeout 2
expect "Password:"
send "$pwd\r"
interact

