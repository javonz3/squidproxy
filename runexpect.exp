#!/usr/bin/expect -f

set user [exec whoami]
set pwd [lindex $argv 0]
spawn \"sudo wget https://raw.githubusercontent.com/hidden-refuge/spi/master/spi && bash spi -ubuntu && sudo rm spi\"
set timeout 2
expect "\[sudo\] password for $user: "
set timeout 2
send "$pwd\r"
set timeout 2
expect "Your desired username: "
send "$user\r"
interact
